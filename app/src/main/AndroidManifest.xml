<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.neopetcarev1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NeoPetCareV1"
        android:usesCleartextTraffic="true">
        <activity android:name=".Perfil_Mascota_Editar"></activity>
        <activity android:name=".InicioMascota" />
        <activity android:name=".ListaDeMascotas" />
        <activity android:name=".RegistroBanios" />
        <activity android:name=".RegistroAlimenticios" />
        <activity android:name=".VacunasActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegistroActiviy" />
        <activity android:name=".AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>